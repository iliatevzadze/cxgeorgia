# Shared packages

Cross-cutting types, constants, and API contracts shared between `apps/frontend`, `apps/backend`, and `apps/worker`.

## Planned contents

- OpenAPI-generated TypeScript types for the frontend
- Shared validation rules or enum definitions
- Versioned API contract documentation

## Status

Placeholder only. Shared packages will be introduced when the first API contracts are defined.
